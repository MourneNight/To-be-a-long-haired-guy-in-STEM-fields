头文件
```c
#include<stdlib.h>
```

使用 rand()  产生随机数
RAND_MAX取决于系统和编译器
使用
```c
printf("%ld\n",RAND_MAX);
```
可查看RAND_MAX大小

rand为伪随机
会打印计算机事先生成的随机数列
传入一个种子
以此种子进行一系列计算
从而获得一串伪随机的数列

srand(seed：) 
传入一个种子来进行伪随机计算
